'use client';
import {useEffect,useState} from 'react';
export default function LaunchExperience(){const[visible,setVisible]=useState(false);const[leaving,setLeaving]=useState(false);useEffect(()=>{if(typeof window==='undefined')return;try{if(sessionStorage.getItem('nfcps-launch-seen-v2')==='1')return;sessionStorage.setItem('nfcps-launch-seen-v2','1')}catch{}setVisible(true);const a=window.setTimeout(()=>setLeaving(true),1050);const b=window.setTimeout(()=>setVisible(false),1450);return()=>{window.clearTimeout(a);window.clearTimeout(b)}},[]);if(!visible)return null;return <div className={leaving?'cx-launch leaving':'cx-launch'} aria-hidden='true'><div className='cx-launch-aura'/><div className='cx-launch-mark'><i/><span/><img src='/resources/nfcps-logo.png' alt=''/></div><div className='cx-launch-copy'><strong>NFCPS One</strong><span>Christ, the Therapy for All.</span></div></div>}
