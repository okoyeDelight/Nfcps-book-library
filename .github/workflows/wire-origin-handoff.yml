name: Wire NFCPS frontend origin handoff

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - .github/workflows/wire-origin-handoff.yml

permissions:
  contents: write

jobs:
  wire:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Validate workflow
        run: echo "Origin handoff workflow is valid"
